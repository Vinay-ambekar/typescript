//marge in interface(interface is only used for objects)
interface Book{
    name:string;
    price:number;
}
interface Book{
    size:number;
}
const book:Book={
    name:'atomic habits',
    price:3132,
    size:75,
    
};